services:
  skillgap_analyzer:
    build: .        # ← builds YOUR custom image from your Dockerfile

    env_file:
      - .env 

    ports:
      - "8000:8000"

    volumes: 
      - upload:/app/uploads
      - output:/app/output

volumes:          
  upload:
  output:







